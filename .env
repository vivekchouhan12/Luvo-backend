
# Backend environment variables
# -- Replace placeholder values with your real secrets before deploying.

# MongoDB connection string (use a read-only or limited user in production)
MONGO_URI="mongodb+srv://root:vroot@vivekchouhan.qpww3tl.mongodb.net/Luvo?appName=VivekChouhan"

# Session secret for `express-session` (keep this secret)
SESSION_SECRET="my secret key"

# Frontend origin allowed for CORS during development
CORS_ORIGIN=http://localhost:5173

# Port used for local development. Vercel provides its own port.
PORT=4000

# Node environment: development | production
NODE_ENV=development
